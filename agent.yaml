agent:
  id: "kimagent"
  team: "cloud"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "strict"

identity:
  sp_client_id: "4ca781dd-6a7b-4b9c-82a0-e01c02cc12b5"
  keyvault_url: "https://kv-shop-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

output_policy:
  violation_threshold: 5
  window_minutes: 3
  action: block
  notify_webhook: 

prompts:
  active_version: v1.0
  source: prompts/system.txt
